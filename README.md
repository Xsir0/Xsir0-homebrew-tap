# Xsir0 Homebrew Tap

Homebrew tap for Xsir0 command-line tools.

## Install codex-meter

```bash
brew tap Xsir0/Xsir0-homebrew-tap https://github.com/Xsir0/Xsir0-homebrew-tap.git
brew install codex-meter
```

Upgrade later:

```bash
brew update
brew upgrade codex-meter
```
